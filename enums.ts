export enum Declaration {
  TYPE_ALIAS = "type",
  INTERFACE = "interface",
  ENUM = "enum",
  EXPORT = "export",
  IMPORT = "import",
  DEFAULT = "default",
  CLASS = "class",
  CONST = "const",
  DECLARE = "declare",
}

export enum DeclarationKind {
  TYPE_ALIAS = "type",
  INTERFACE = "interface",
  ENUM = "enum",
}

export enum TypeKind {
  NUMBER = "number",
}
